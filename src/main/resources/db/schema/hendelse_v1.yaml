name: hendelse_v1
description: "Generisk tabell for hendelser"
fields:
  - name: opprettet
    type: DATETIME
    mode: REQUIRED
    description: "Dato og klokkeslett for hendelsen"
    maxLength:
  - name: navn
    type: STRING
    mode: REQUIRED
    description: "Navn på hendelsen"
    maxLength:
  - name: kilde
    type: STRING
    mode: REQUIRED
    description: "Systemet hendelsen oppsto i"
    maxLength:
  - name: stikkord
    type: STRING
    mode: REPEATED
    description: "Stikkord for hendelsen"
    maxLength:
  - name: data
    type: JSON
    mode: NULLABLE
    description: "Tilleggsinformasjon til hendelsen"
    maxLength:
  - name: tidsstempel
    type: TIMESTAMP
    mode: REQUIRED
    description: "Tidsstempel for lagring av hendelsen"
    maxLength:
